<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.orders.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.example.orders.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="email" column="email"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>

    <insert id="insertUser" parameterType="com.example.orders.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, email, phone_number, status, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{email}, #{phoneNumber}, #{status}, NOW(), NOW())
    </insert>

    <select id="selectUserById" parameterType="java.lang.Long" resultMap="userResultMap">
        SELECT id, username, password_hash, email, phone_number, status, created_at, updated_at, last_login_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectUserByUsername" parameterType="java.lang.String" resultMap="userResultMap">
        SELECT id, username, password_hash, email, phone_number, status, created_at, updated_at, last_login_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectAllUsers" resultMap="userResultMap">
        SELECT id, username, password_hash, email, phone_number, status, created_at, updated_at, last_login_at
        FROM users
    </select>

    <update id="updateUser" parameterType="com.example.orders.entity.User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="passwordHash != null">password_hash = #{passwordHash},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteUserById" parameterType="java.lang.Long">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <update id="updateLastLoginTime">
        UPDATE users
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{id}
    </update>

</mapper>
